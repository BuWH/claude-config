# CLAUDE.md

## Environment
* When need environmental variables, check `.env` first
* When need read variables, read the `.env` in this directory first
* When you need to install python package, use virtual env. Use `uv` to manage python environment
* Always use `bun` to manage node dependency